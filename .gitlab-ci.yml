image: node:latest

cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
  - node_modules/

before_script:
- npm ci

lint:
  script:
  - npm run lint

pages:
  script:
  - mv src public
  artifacts:
    paths:
    - public
  only:
  - master
